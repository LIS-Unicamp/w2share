AppBundle\Entity\OutputRun: 
    type: entity
    fields:   
        uri:
            type: string
        content:
            type: string
    manyToOne:
        process_run:
            targetEntity: ProcessRun
            inversedBy: outputs
        workflow_run:
            targetEntity: WorkflowRun
            inversedBy: outputs