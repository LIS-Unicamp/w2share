AppBundle\Entity\Input: 
    type: entity
    fields:   
        uri:
            type: string
        description:
            type: string
        label:
            type: string
        example_data:
            type: string
    manyToOne:
        process:
            targetEntity: Process
            inversedBy: inputs
        workflow:
            targetEntity: Workflow
            inversedBy: inputs