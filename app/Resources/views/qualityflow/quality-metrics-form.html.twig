{% extends 'base.html.twig' %}
{% block title %}Add Quality Metric{% endblock %}
{% block body %}

<div>
<h2>{% if qualityMetric.uri %}Edit{% else %}Add{% endif %} <small>quality metric</small></h2>
{{ form_start(form) }}
    <div class="form-body">
        {% for flashMessage in app.session.flashbag.get('success') %}
            <div class="alert alert-success">
                    <button class="close" data-dismiss="alert"></button>
                    {{ flashMessage }}
            </div>            
        {% endfor %}
        {% for flashMessage in app.session.flashbag.get('error') %}
            <div class="alert alert-danger">
                    <button class="close" data-dismiss="alert"></button>
                    {{ flashMessage }}
            </div>            
        {% endfor %}
        {{ form_errors(form) }}
        <div class="row">
            <div class="col-md-12">
                <div class="form-group">
                    {{ form_label(form.metric) }}
                    {{ form_widget(form.metric, { 'attr': { 'class':'form-control' } }) }}
                    {{ form_errors(form.metric) }}
                </div>
             </div>
            <div class="col-md-12">
                <div class="form-group">
                    {{ form_label(form.description) }}
                    {{ form_widget(form.description, { 'attr': { 'class':'form-control' } }) }}
                    {{ form_errors(form.description) }}
                </div>
            </div>
        </div>
    </div>
    <div class="form-actions">
        {% if qualityMetric.uri %} 
            {{ form_widget(form.save, { 'label':'Edit' }) }} 
        {% else %}
            {{ form_widget(form.save, { 'label':'Add' }) }}
        {% endif %}
        <a href="" class="btn btn-info">List</a>
    </div>
    {{ form_end(form) }}
</div>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Actions</th>
                <th>Dimension</th>
                <th>Metric</th>
                <th>Description</th>
                <th>Author</th>
            </tr>
        </thead>
        <tbody>
            {% for metric in pagination %}
                <tr>
                    <td>
                        <a href="" 
                           title="Delete quality metric" ><i class="glyphicon glyphicon-trash"></i></a> 
                        <a href="" 
                           title="Edit quality annotations"><i class="glyphicon glyphicon-edit"></i></a> 
                    </td>
                    <td>{{ metric.qualityDimension.name }}</td>
                    <td>{{ metric.metric }}</td>
                    <td>{{ metric.description }}</td>
                    <td>{{ metric.creator }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div> 
<div class="row">
    <div class="col-sm-12">
        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
</div>
<div class="row">
    <div class="col-sm-12">
        {{ app_pagination_render(pagination) }}
    </div>
</div>
    
{% endblock %}  
{% block javascripts %}
{% endblock %}
{% block stylesheets %}
{% endblock %} 